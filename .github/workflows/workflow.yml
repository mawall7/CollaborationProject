name: Build DLL on Pull Request

on:
  push:
    branches:
      - main   

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile DLL
        shell: powershell
        run: |
          # Compile test.cpp to libTest.dll in Collaboration folder
          g++ -shared -o CollaborationProject\libTest.dll CPPProj\test.cpp

      - name: Commit and push DLL
        shell: powershell
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add Collaboration\libTest.dll
          git commit -m "Auto-build DLL on PR" || exit 0
          git push
        env:  
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
